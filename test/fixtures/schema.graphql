input DoStuffInput {
  name: String!
  clientMutationId: String
}

type DoStuffPayload {
  result: String
  clientMutationId: String
}

type Mutation {
  doStuff(input: DoStuffInput!): DoStuffPayload
}

type Query {
  dummy: ID
}
